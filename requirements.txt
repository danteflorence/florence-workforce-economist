# Pinned to tested-major windows. An unpinned resolve on Python 3.11 (July 2026:
# pandas 3.0 / numpy 2.4 / pyarrow 25) segfaults in the pandas↔pyarrow native
# stack — caught by CI run #1. Bump these caps deliberately, with CI green.
streamlit>=1.50,<1.60    # st.context.cookies (Core SSO) + st.login; AppTest-tested at 1.50
PyJWT[crypto]>=2.8       # verify FlorenceRN Core RS256 fl_session cookie (core_auth.py)
pandas>=2.2,<3           # pandas 3.x untested — known segfault with pyarrow 25 combo
numpy>=1.26,<3
pyarrow>=16,<22          # parquet engine; tested at 21
openpyxl>=3.1,<4
reportlab>=4.0,<5        # 5.0 changed APIs the PDF exporters use
python-pptx>=1.0,<2
plotly>=5.18,<7
requests>=2.31
statsmodels>=0.14,<0.15
anthropic>=0.30
PyMuPDF>=1.23,<2
Authlib>=1.3.2
google-api-python-client>=2.100
google-auth>=2.30
google-auth-oauthlib>=1.2
dnspython>=2.6
qrcode[pil]>=7.4         # postcard QR codes (florence_postcard.py); pulls in Pillow
sentry-sdk>=2.0          # error monitoring (error_monitoring.py); no-op without SENTRY_DSN
